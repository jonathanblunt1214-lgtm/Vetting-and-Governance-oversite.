import crypto from 'node:crypto';import fs from 'node:fs';import path from 'node:path';
const sha=(value)=>crypto.createHash('sha256').update(value).digest('hex');
const exact=(value,keys,label)=>{if(!value||typeof value!=='object'||Array.isArray(value)||Object.keys(value).sort().join(',')!==[...keys].sort().join(','))throw new Error(`${label} schema is invalid.`);};
const digest=(value)=>typeof value==='string'&&/^[a-f0-9]{64}$/.test(value);
export function vetManifest(value){exact(value,['schemaVersion','candidateId','projectId','claim','claimBoundary','classification','sources','submittedAt'],'candidate');if(value.schemaVersion!==1||value.classification!=='Insufficient Evidence'||![value.candidateId,value.projectId,value.claim,value.claimBoundary].every((item)=>typeof item==='string'&&item.length>0&&item.length<=2000)||!Array.isArray(value.sources)||!value.sources.length||!Number.isFinite(Date.parse(value.submittedAt)))throw new Error('Candidate identity, boundary, classification, sources, or time is invalid.');for(const source of value.sources){exact(source,['sourceId','url','author','license','retrievedAt','contentSha256'],'source');if(!/^https:\/\//.test(source.url)||!digest(source.contentSha256)||![source.sourceId,source.author,source.license].every((item)=>typeof item==='string'&&item.length>0)||!Number.isFinite(Date.parse(source.retrievedAt)))throw new Error('Source provenance is invalid.');}return{candidateId:value.candidateId,state:'structurally-vetted-candidate-only',classification:value.classification,sourceCount:value.sources.length,manifestSha256:sha(JSON.stringify(value)),proofStageSatisfied:false,promotionAuthorized:false};}
export function auditCrucible(root){const required=['AGENTS.md','AI-HANDOFF.json','AI-CONFLICTS.json','DEVLOG.md','governingDocuments/scientific-learning-policy.md','governingDocuments/host-isolation-policy.md','src/scientificLearning.js','src/hostIsolation.js','src/externalAiFirewall.js'];const missing=required.filter((file)=>!fs.existsSync(path.join(root,file)));const handoff=missing.includes('AI-HANDOFF.json')?null:JSON.parse(fs.readFileSync(path.join(root,'AI-HANDOFF.json'),'utf8'));const conflicts=missing.includes('AI-CONFLICTS.json')?null:JSON.parse(fs.readFileSync(path.join(root,'AI-CONFLICTS.json'),'utf8'));const findings=[];if(missing.length)findings.push({kind:'missing-governance',paths:missing});if(handoff?.activePlan?.status!=='active')findings.push({kind:'handoff-not-active'});if(!Array.isArray(conflicts?.conflicts))findings.push({kind:'invalid-conflict-ledger'});return{state:findings.length?'failed':'passed',findings,requiredChecked:required.length};}
export function run({targetRoot,inboxRoot,commitSha}){const manifests=[];if(fs.existsSync(inboxRoot))for(const name of fs.readdirSync(inboxRoot).filter((item)=>item.endsWith('.json')).sort())try{manifests.push(vetManifest(JSON.parse(fs.readFileSync(path.join(inboxRoot,name),'utf8'))));}catch(error){manifests.push({file:name,state:'rejected',reason:error.message});}const report={schemaVersion:1,crucibleCommit:commitSha,oversight:auditCrucible(targetRoot),manifests,generatedAt:new Date().toISOString(),independent:true,proofStageSatisfied:false};fs.writeFileSync('oversight-report.json',`${JSON.stringify(report,null,2)}\n`);if(report.oversight.state!=='passed'||manifests.some((item)=>item.state==='rejected'))process.exitCode=1;return report;}
if(import.meta.url===`file:///${process.argv[1]?.replaceAll('\\','/')}`){run({targetRoot:path.resolve(process.argv[2]),inboxRoot:path.resolve(process.argv[3]),commitSha:process.argv[4]});}
